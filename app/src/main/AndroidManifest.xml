<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.volvo.wis.pbv">
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_volvo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".app.MyApplication"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.volvo.wis.pbv.activities.LoginActivity"
            android:screenOrientation="fullSensor"
            android:theme="@android:style/Theme.Material.NoActionBar.Fullscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name="com.volvo.wis.pbv.activities.KitActivity"
            android:screenOrientation="fullSensor"
            android:theme="@style/AppTheme">
        </activity>
        <activity
            android:name="com.volvo.wis.pbv.activities.PickingActivity"
            android:screenOrientation="fullSensor">
        </activity>
    </application>
</manifest>